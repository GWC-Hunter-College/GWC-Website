import AnimatedHeroLogo from "./AnimatedHeroLogo";

/** Replace this component's internals when the final animated SVG is ready. */
const AnimatedHeroBrand = () => <AnimatedHeroLogo />;

export default AnimatedHeroBrand;
